node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
.pnpm-store/
coverage/
*.tsbuildinfo
packages/db/prisma/migrations/.gitkeep

# F4.8: robots.txt/sitemap.xml se generan en cada dev/build a partir de
# BUSINESS_DOMAIN (ver apps/marketing/scripts/generate-seo-files.mjs) —
# nunca se commitean, para que el dominio real nunca quede hardcodeado
# en el repo.
apps/marketing/public/robots.txt
apps/marketing/public/sitemap.xml

# F4.9-11: salida de Playwright (traces/screenshots de fallos) — nunca
# se commitea, se regenera en cada corrida.
apps/web/test-results/
apps/web/playwright-report/

# F12.6: dumps reales de backup -- nunca se commitean (contienen datos
# reales de negocio), ver scripts/db-backup.sh y
# docs/BACKUP_AND_RESTORE.md.
backups/
*.dump
